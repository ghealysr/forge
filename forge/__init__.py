"""
FORGE — Free Open-source Runtime for Gemma Execution

An agentic loop system that brings proven agent architecture patterns
to local and cloud models for autonomous data enrichment.

Usage:
    python -m forge --mode both --state CA --max 1000
    python -m forge --mode email --workers 30
    python -m forge --mode ai --batch-size 5
"""

__version__ = "2.0.0"
